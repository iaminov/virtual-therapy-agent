[tool.poetry]
name = "virtual-therapist"
version = "0.1.0"
description = "AI-powered therapeutic conversation system"
authors = ["iaminov"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
